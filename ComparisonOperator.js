///The Comparison operator is two types

// Equality Operator (==) is used on  true and false values /// in this operator only enter the user defined values
// Strict Equality operator (===) is used true and false values as well as data types /// in  this operator its atomaticlly defined values and data types



let a = 10;
console.log(a == 10);// true 


let b = 20;
let c = 20;

console.log(b == c);


let d = "20";
let e = 20;

console.log(d === e);


 ////*In the == is the value is compared only 
////* In the === is the datatype and value is compared 

let f = 80;
let g = true;

console.log(f === g); // In the condition  the data type and value is differnt thats why its showing false



let h = 1;
let i = true;

console.log(h == i); // In This condition the value is same in the both thats why its the condition is true 
